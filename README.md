# gamesApi

ASP.NET Core Minimal API + статична сторінка з картою.

## API

| Method | Route | Description |
|--------|-------|-------------|
| GET | `/api/map/pins` | Точки для карти (JSON) |

## Запуск

```bash
dotnet run
```

Відкрийте `/` або `/admin/index.html`.
